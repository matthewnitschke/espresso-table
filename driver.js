// const power = require("pi-pins").connect(17);

// power.mode('low');

module.exports = {
    on: async () => {
        // power.value(true);
    },
    off: async () => {
        // power.value(false);
    }
}